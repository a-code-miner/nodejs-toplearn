<!DOCTYPE html>
<html lang="fa-IR" dir="rtl">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="css/style.css" />
    <link rel="stylesheet" href="css/bootstrap.rtl.css" />
    <link rel="stylesheet" href="css/font-awesome.css" />
    <title>
        <%= pageTitle %>
    </title>
</head>

<body>
    <div class="form-group">
        <h1>کارهای <small>روزمره</small></h1>
        <form action="/admin/add-todo" role="form" class="row w-50 justify-content-center mx-auto" method="POST">
            <div class="input-group">
                <input type="text" name="todo" class="form-control w-50" placeholder="کار امروز" />
                <div class="input-group-prepend">
                    <button type="submit" class="btn btn-primary addButton fa fa-plus" />
                </div>
            </div>
        </form>
        <div class="mx-auto w-50 mb-3 alert alert-primary row">
            <p class="d-inline col">کارهای انجام شده : <span
                    class="badge rounded-pill bg-success">0<!-- CompletedTodos --></span>
            </p>
            <p class="d-inline col">کارهای باقی مانده امروز : <span
                    class="badge rounded-pill bg-warning">0<!-- RemainingTodos --></span></p>
        </div>
        <% if (todos.length> 0) { %>
            <ul class="list-unstyled w-50 mx-auto" id="todo">
                <% for (let todo of todos) { %>
                    <li class="row justify-content-between align-items-center bg-info mb-3 p-2 rounded">
                        <div class="col-5">
                            <span class="todo-text">
                                <%= todo.text %>
                            </span>
                        </div>
                        <div class="col-2 text-right d-flex">
                            <form action="/admin/delete-todo" method="POST" class="m-2">
                                <input type="hidden" name="todoId" value="<%= todo.id %>" />
                                <button type="submit" class="btn btn-danger fa fa-trash"></button>
                            </form>
                            <form action="/admin/complete-todo" method="POST" class="m-2">
                                <input type="hidden" name="todoId" value="<%= todo.id %>" />
                                <button type="submit" class="btn btn-success fa fa-check"></button>
                            </form>
                            <% } %>
            </ul>
            <% } else { %>
                <div class="alert alert-warning w-50 mx-auto" role="alert">
                    <h4 class="text-center">هنوز کاری برای انجام دادن ثبت نشده است.</h4>
                    <p>می‌توانی کارهای امروزت را بالا بنویسی تا برایت ثبت کنم</p>
                </div>
                <% } %>
    </div>
</body>

</html>